// This file intentionally re-exports from the non-dynamic path
// The actual product page is in app/product/id/page.js for file system compatibility
// In a real deployment, rename the folder from "id" to "[id]"
export { default } from '../id/page';
